#include <gtest/gtest.h>
#include "../Mock_Database.h"



// TEST(DatabaseTest, TestQuery)
// {
//     Mock_Database mock_db;

//     // EXPECT_CALL(mock_db, connect("some_connection_string"))
//     //     .WillOnce(testing::Return(true));

//     // //-- ??
//     // EXPECT_CALL(mock_db, executeQuery("SELECT * FROM users"))
//     //     .WillOnce(testing::Return(std::vector<std::vector<std::string>>{{"1", "Alice"}, {"2", "Bob"}}));

//     // // Тестируемый код
//     // bool isConnected = mock_db.connect("some_connection_string");
//     // ASSERT_TRUE(isConnected);

//     // auto results = mock_db.executeQuery("SELECT * FROM users");
//     // ASSERT_EQ(results.size(), 2);
//     // ASSERT_EQ(results[0][1], "Alice");
// }
